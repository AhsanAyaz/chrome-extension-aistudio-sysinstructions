---
phase: 05-popup-badge-export-import
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - wxt.config.ts
autonomous: true
requirements: [UI-01, UI-02, UI-03, UI-04, UI-05, UI-06, EXPORT-01, EXPORT-02]

must_haves:
  truths:
    - "svelte 5.55.5 and @wxt-dev/module-svelte 2.0.5 appear in package.json dependencies"
    - "wxt.config.ts contains modules: ['@wxt-dev/module-svelte']"
    - "npx wxt build exits 0 after the config change"
  artifacts:
    - path: "package.json"
      provides: "svelte + @wxt-dev/module-svelte in devDependencies (or dependencies)"
      contains: "@wxt-dev/module-svelte"
    - path: "wxt.config.ts"
      provides: "Svelte module declaration for WXT (D-11)"
      contains: "modules: ['@wxt-dev/module-svelte']"
  key_links:
    - from: "wxt.config.ts"
      to: "@wxt-dev/module-svelte"
      via: "modules array"
      pattern: "modules.*module-svelte"
---

<objective>
Install the Svelte build dependency and wire it into WXT's module system so the subsequent popup plans can create .svelte files that compile correctly.

Purpose: D-11 — the placeholder comment in wxt.config.ts ("Svelte is Phase 5") is replaced with the actual modules declaration. This is a prerequisite for all popup plans.
Output: Updated package.json, updated wxt.config.ts, verified build.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-popup-badge-export-import/05-CONTEXT.md
@.planning/phases/05-popup-badge-export-import/05-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install Svelte + WXT Svelte module</name>
  <files>package.json</files>
  <read_first>
    - package.json (current devDependencies to confirm svelte/module-svelte are absent)
  </read_first>
  <action>
Run the following install commands in the project root:

```bash
npm install svelte
npm install --save-dev @wxt-dev/module-svelte
```

Expected versions: svelte@5.55.5, @wxt-dev/module-svelte@2.0.5 (these are pinned per RESEARCH.md — if npm resolves different patch versions that is acceptable; major version must be 5.x / 2.x respectively).

Do NOT install the `uuid` npm package — crypto.randomUUID() is used throughout this codebase.
  </action>
  <verify>
    <automated>node -e "const p=require('./package.json'); console.log(p.dependencies?.svelte || p.devDependencies?.svelte); console.log(p.devDependencies?.['@wxt-dev/module-svelte'])"</automated>
  </verify>
  <done>Both svelte and @wxt-dev/module-svelte appear in package.json with non-empty version strings.</done>
</task>

<task type="auto">
  <name>Task 2: Add Svelte module to wxt.config.ts (D-11)</name>
  <files>wxt.config.ts</files>
  <read_first>
    - wxt.config.ts (read current content before editing — the Phase 5 placeholder comment must be replaced)
  </read_first>
  <action>
Edit wxt.config.ts. Replace the comment line:

```
// No `modules: ['@wxt-dev/module-svelte']` in Phase 1 — Svelte is Phase 5.
```

with the actual modules declaration, so the config reads:

```typescript
export default defineConfig({
  srcDir: 'src',
  entrypointsDir: '.', // matches D-23 layout: src/background/, src/content/, src/popup/
  modules: ['@wxt-dev/module-svelte'],  // Phase 5 addition (D-11)
  manifest: {
    // ... rest unchanged
  },
  // ... hooks unchanged
});
```

The `modules` line goes between `entrypointsDir` and `manifest`. All other fields remain exactly as they are. Do NOT add any new manifest permissions — the permissions list is correct as-is (no 'downloads' permission is needed because export uses Blob + anchor click per RESEARCH.md).

After editing, run a build smoke test:
```bash
npx wxt build 2>&1 | tail -20
```

If the build fails because `src/popup/` doesn't exist yet, that is acceptable — the error should be "popup entrypoint not found" not a module compilation error. The critical check is that the `@wxt-dev/module-svelte` module loads without error.
  </action>
  <verify>
    <automated>grep -n "modules.*module-svelte" wxt.config.ts</automated>
  </verify>
  <done>wxt.config.ts contains `modules: ['@wxt-dev/module-svelte']` and the old placeholder comment is removed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → local install | Third-party package installed; only svelte (meta-framework) and WXT plugin installed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01-01 | Tampering | npm dependency install | accept | Only two packages from well-known publishers (sveltejs/svelte, wxt-dev); version pinned to known-good via RESEARCH.md |
</threat_model>

<verification>
1. `grep "@wxt-dev/module-svelte" package.json` returns a line with a version
2. `grep "svelte" package.json` returns a line with a version (not just @wxt-dev/module-svelte)
3. `grep "modules.*module-svelte" wxt.config.ts` returns one matching line
4. `npx tsc --noEmit` exits 0 (wxt.config.ts change must not break TypeScript)
</verification>

<success_criteria>
- package.json contains both svelte and @wxt-dev/module-svelte
- wxt.config.ts has `modules: ['@wxt-dev/module-svelte']` (D-11 satisfied)
- No TypeScript errors introduced
</success_criteria>

<output>
After completion, create `.planning/phases/05-popup-badge-export-import/05-01-SUMMARY.md`
</output>
